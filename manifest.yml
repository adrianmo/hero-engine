---
applications:
- name: hero-engine
  host: hero-engine-dev
  command: hero-engine
  memory: 512M
  services:
    - hero-mysql
  env:
    GOPACKAGENAME: github.com/vxrackneutrino/hero-engine
    ADMIN_TOKEN: 1234
